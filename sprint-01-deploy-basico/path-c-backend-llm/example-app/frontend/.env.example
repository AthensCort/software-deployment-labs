# Variables del FRONTEND — visibles en el navegador
# NUNCA poner API keys de pago aquí

# URL del backend en Railway (cambiar en producción)
VITE_BACKEND_URL=http://localhost:3001
VITE_ENV_NAME=development
